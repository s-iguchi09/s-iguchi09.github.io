# 全ページのクロールを許可する。
# これはクロールの許可であって、インデックス登録を保証するものではない。
#
# コントロール別デモページは 2026-07-25 に AdSense のコンテンツクローラ
# (Mediapartners-Google) から除外していたが、2026-08-29 に解除した。
# 同時に行っていた noindex 化を含め、Search Console の実測では審査結果に寄与せず、
# 検索流入（サイト全体の表示回数の 41%）を削るだけの結果に終わったため。
# 経緯は _includes/head.html のコメントに記録している。
#
# 個別ページをインデックスから外す場合は、robots.txt の Disallow ではなく
# front matter の `noindex: true` を使うこと。Disallow にすると noindex メタ自体が
# 読まれず、URL だけがインデックスされうる。
User-agent: *
Allow: /

Sitemap: https://s-iguchi09.github.io/sitemap_index.xml
Sitemap: https://s-iguchi09.github.io/sitemap.xml
