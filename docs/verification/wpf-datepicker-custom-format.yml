# tools/screenshot-capture が実行時に自動生成する。手で編集しない。
# 再生成: dotnet run --project tools/screenshot-capture -c Release -- wpf-datepicker-custom-format
slug: "wpf-datepicker-custom-format"
scene: "DatePickerFormatScene"
environment:
  runtime: ".NET 10.0.10"
  os: "Microsoft Windows 10.0.26200"
verifies:
  - "SelectedDateFormat が Short と Long の 2 つしか持たず、任意の書式にできないこと"
  - "SelectedDateFormat を設定しない場合の実効値が Short であり、それが依存関係プロパティのメタデータ既定値（Long）ではなく既定スタイル由来であること"
  - "それぞれの設定で実際に表示される文字列"
  - "テンプレート内のテキスト部分を書き換えると任意の書式にできること"
  - "既定表示のカルチャが要素の Language（xml:lang）とスレッドの CurrentCulture のどちらに従うか（自身への指定・親要素からの継承・CurrentCulture を独立に変えて表示文字列を測る）"
images:
  - "images/articles/wpf-datepicker-custom-format/datepicker-default-vs-custom-format.png"
  - "images/articles/wpf-datepicker-custom-format/datepicker-format-matrix.svg"
  - "images/articles/wpf-datepicker-custom-format/datepicker-culture-matrix.svg"
