# tools/screenshot-capture が実行時に自動生成する。手で編集しない。
# 再生成: dotnet run --project tools/screenshot-capture -c Release -- wpf-textbox-updatesource-from-view-pitfalls
slug: "wpf-textbox-updatesource-from-view-pitfalls"
scene: "UpdateSourcePitfallScene"
environment:
  runtime: ".NET 10.0.10"
  os: "Microsoft Windows 10.0.26200"
verifies:
  - "GetBindingExpression が null を返す条件（リテラル・MultiBinding・TemplateBinding）"
  - "バインドを張ったまま UpdateSource を呼んだ場合と、Text を書き換えてから呼んだ場合の違い"
  - "OneWay / OneTime では Text への代入がバインドを外し、その後の UpdateSource が例外になること"
images:
  - "images/articles/wpf-textbox-updatesource-from-view-pitfalls/updatesource-pitfall-matrix.png"
