# tools/screenshot-capture が実行時に自動生成する。手で編集しない。
# 再生成: dotnet run --project tools/screenshot-capture -c Release -- wpf-observablecollection-cross-thread-update
slug: "wpf-observablecollection-cross-thread-update"
scene: "CollectionCrossThreadScene"
environment:
  runtime: ".NET 10.0.10"
  os: "Microsoft Windows 10.0.26200"
verifies:
  - "バックグラウンドスレッドからの Add で送出される例外を確かめる"
  - "バインドしていない ObservableCollection では例外にならないこと（原因が CollectionView 側にある証拠）"
  - "Dispatcher.Invoke と EnableCollectionSynchronization のいずれでも例外が消えること"
  - "EnableCollectionSynchronization は、UI スレッドでバインド前に登録し、登録したのと同じロックで Add を包んだ構成で測っている"
  - "この構成では変更と CollectionChanged 通知が同じロックの中で起きるため、UI スレッド側の列挙と競合しないこと"
images:
  - "images/articles/wpf-observablecollection-cross-thread-update/collection-cross-thread-matrix.svg"
